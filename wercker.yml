box: eymengunay/php55@0.0.7
services:
  - wercker/mysql@0.0.8
## Build definition
build:
  # The steps that will be executed on build
  steps:
  - vinietje/phplint-build:
        php_directory: $DIRECTORY
    # A step that executes `npm install` command
deploy:
  steps:
